import React from 'react';
import useStyles from './stylesAuth';

const Auth = () => {

  const classes = useStyles();
  const state = null;

  return (
    <div>
      AUTH
    </div>
  );
};

export default Auth;